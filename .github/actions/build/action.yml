name: "Build inside docker"
description: 'Install deps and build latex file'
runs:
  using: 'docker'
  image: '../../../Dockerfile'
  volume:
  
  args:
    - --mount type=bind,source=./source,target=/src/source
